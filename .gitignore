# virtualenv / caches
.venv/
__pycache__/
*.pyc
.pytest_cache/
.cache/

# generated output
/out/
/library/
chapter.html
*.opus
*.wav
*.m4b
*.m4a
*.mp3
*.epub
*.segments.json

# local state / config
*.db
config.toml
config.local.toml

# per-series lexicons / overrides for real (private) series —
# keep only the shipped base + the synthetic fixture
/data/lexicons/*.csv
!/data/lexicons/_base.csv
!/data/lexicons/salvage-run.csv
/data/series/*.toml

# editor / OS
.DS_Store
*.swp
*~
